diff --git a/node_modules/@deepseek-ai/dsh-session-persistence/lib/index.js b/node_modules/@deepseek-ai/dsh-session-persistence/lib/index.js
--- a/node_modules/@deepseek-ai/dsh-session-persistence/lib/index.js
+++ b/node_modules/@deepseek-ai/dsh-session-persistence/lib/index.js
@@ -45,6 +45,11 @@
 	*/
 	has(id) {
 		return this.entries.has(id);
+	}
+	/** Reject deletion while an unpublished Session exclusively owns the id. */
+	assertDeletable(id) {
+		const phase = this.entries.get(id)?.phase;
+		if (phase === "committing" || phase === "reserved") throw new Error(`cannot delete session "${id}" while its persisted preparation is reserved`);
 	}
 	/**
 	* Observe one prepared source, sharing an in-flight read for the same id.
@@ -940,6 +945,22 @@
 		state.cursor = nextCursor;
 		this.preparations.invalidate(id);
 	}
+	/** Permanently remove a cold Session identity after all owned writes retire. */
+	async delete(id) {
+		await this.waitForRetirement(id);
+		if (this.ctx.sessions.get(id) !== void 0) throw new Error(`cannot delete session "${id}" while it is live`);
+		return this.serialize(id, async () => {
+			if (this.ctx.sessions.get(id) !== void 0) throw new Error(`cannot delete session "${id}" while it is live`);
+			this.preparations.assertDeletable(id);
+			const tracked = this.states.get(id);
+			if (this.backend.deleteStored === void 0) throw new Error("this session persistence backend does not support deletion");
+			const deleted = await this.backend.deleteStored(id);
+			if (!deleted && tracked?.materialized === true) throw new Error(`cannot delete session "${id}": its tracked durable artifact is missing`);
+			this.states.delete(id);
+			this.preparations.invalidate(id);
+			return deleted || tracked !== void 0;
+		});
+	}
 	/**
 	* Prepare and reserve the exact unpublished Session used by resume.
 	* Revision retries converge once the durable log remains unchanged for one
@@ -1566,6 +1587,10 @@
 	ensureMaterialized(_session) {
 		return Promise.reject(/* @__PURE__ */ new Error("this session persistence backend cannot materialize an empty session"));
 	}
+	/** Permanently remove one cold Session identity when the backend supports deletion. */
+	delete(_id) {
+		return Promise.reject(/* @__PURE__ */ new Error("this session persistence backend does not support deletion"));
+	}
 	/**
 	* Prepare the exact unpublished Session used by resume. Implementations may
 	* reuse object graphs retained by an earlier {@link inspect} after confirming
diff --git a/node_modules/@deepseek-ai/dsh-session-persistence/lib/types/coordinator.d.ts b/node_modules/@deepseek-ai/dsh-session-persistence/lib/types/coordinator.d.ts
--- a/node_modules/@deepseek-ai/dsh-session-persistence/lib/types/coordinator.d.ts
+++ b/node_modules/@deepseek-ai/dsh-session-persistence/lib/types/coordinator.d.ts
@@ -148,6 +148,8 @@
      * inherited prefix length.
      */
     appendBatch(storage: SessionStorageMetadata, events: readonly SessionEvent[], isMaterialized: boolean): Promise<void>;
+    /** Permanently remove one stored Session artifact. */
+    deleteStored?(id: SessionId): Promise<boolean>;
     /**
      * Make a crash repair durable: truncate the torn tail (iff
      * `tornMarker !== undefined`) and append `closers` (iff any). NOT required to
@@ -229,6 +231,8 @@
      */
     append(id: SessionId, events: readonly SessionEvent[]): Promise<void>;
     private appendCore;
+    /** Permanently remove a cold Session identity after all owned writes retire. */
+    delete(id: SessionId): Promise<boolean>;
     /**
      * Prepare and reserve the exact unpublished Session used by resume.
      * Revision retries converge once the durable log remains unchanged for one
diff --git a/node_modules/@deepseek-ai/dsh-session-persistence/lib/types/index.d.ts b/node_modules/@deepseek-ai/dsh-session-persistence/lib/types/index.d.ts
--- a/node_modules/@deepseek-ai/dsh-session-persistence/lib/types/index.d.ts
+++ b/node_modules/@deepseek-ai/dsh-session-persistence/lib/types/index.d.ts
@@ -143,6 +143,8 @@
      * @param events - the contiguous batch to persist, in seq order.
      */
     abstract append(id: SessionId, events: readonly SessionEvent[]): Promise<void>;
+    /** Permanently remove one cold Session identity when the backend supports deletion. */
+    delete(_id: SessionId): Promise<boolean>;
     /**
      * Prepare the exact unpublished Session used by resume. Implementations may
      * reuse object graphs retained by an earlier {@link inspect} after confirming
diff --git a/node_modules/@deepseek-ai/dsh-session-persistence/lib/types/preparations.d.ts b/node_modules/@deepseek-ai/dsh-session-persistence/lib/types/preparations.d.ts
--- a/node_modules/@deepseek-ai/dsh-session-persistence/lib/types/preparations.d.ts
+++ b/node_modules/@deepseek-ai/dsh-session-persistence/lib/types/preparations.d.ts
@@ -39,6 +39,8 @@
      * @returns whether an entry exists for the identity.
      */
     has(id: SessionId): boolean;
+    /** Reject deletion while an unpublished Session exclusively owns the id. */
+    assertDeletable(id: SessionId): void;
     /**
      * Observe one prepared source, sharing an in-flight read for the same id.
      * @param id - session identity.
